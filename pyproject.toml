[tool.poetry]
name = "shortener"
version = "0.1.0"
description = ""
authors = ["vadim.rudenkov <vadim.rudenkov@muehlemann-popp.ch>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
